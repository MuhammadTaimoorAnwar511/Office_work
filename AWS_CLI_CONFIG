aws configure (which cli user is connected)
aws configure --profile=ecs-ecr-user (to create new user for cli access)
cat ~/.aws/credentials (list of current cli users)
export AWS_PROFILE=ecs-ecr-user (to switch user)
